{
  "name": "alpha-market",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "api": "pnpm --filter @alpha/api dev",
    "web": "pnpm --filter @alpha/web dev",
    "typecheck": "pnpm -r typecheck",
    "hedera:test": "pnpm --filter @alpha/api hedera:test"
  },
  "engines": { "node": ">=20" }
}
